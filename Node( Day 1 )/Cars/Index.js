// In your module file (e.g., index.js)
const First = require('./First');
const Second = require('./Second');
const Third = require('./Third');

const info = { First, Second, Third };

module.exports = { info };
