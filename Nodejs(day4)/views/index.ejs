<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Personal Thought</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #2b2b2b;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        a {
        background-color: #ff5555;
        color: #fff;
        padding: 10px 20px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        display: block;
        margin: 20px auto;
        font-size: 16px;
        text-decoration: none;
        display: inline-block;
    }

        #feed-container {
            width: 600px;
            background-color: #333;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            margin-top: 20px;
        }

        #header {
            background-color: #1c1c1c;
            color: #ff5555;
            text-align: center;
            padding: 20px;
            margin: 0;
            font-size: 24px;
        }

        .post {
            padding: 20px;
            border-bottom: 1px solid #444;
        }

        .post:hover {
            background-color: #3d3d3d;
        }

        .username {
            color: #ff5555;
            margin-bottom: 8px;
            font-size: 18px;
        }

        .post-content {
            color: #ddd;
            margin-bottom: 16px;
            font-size: 16px;
        }

        .create-post-button {
            background-color: #ff5555;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            display: block;
            margin: 20px auto;
            font-size: 16px;
            text-decoration: none;
            display: inline-block;
        }

        .create-post-button:hover {
            background-color: #cc0000;
        }
    </style>
</head>
<body>
    <div id="feed-container">
        <div id="header">Personal Thought Feed</div>
        <% for(post of posts){ %>
            <div class="post">
                <div class="username">@ <%= post.username %></div>
                <div class="post-content"><%= post.postContent %></div>
                <a href="http://localhost:8000/post/<%= post.id %>">See in details</a>
            </div>
        <%  }%> 
        <a href="http://localhost:8000/post/new" class="create-post-button">Create post</a>
    </div>
</body>
</html>
